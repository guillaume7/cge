# TH8.E3 — Attribution and Copilot CLI Integration

Generate, persist, and retrieve attribution-first decision records (ADR-021) and
wire the full evaluator loop (Context Evaluator → Decision Engine → Attribution
Recorder) into the existing `graph context` and `graph workflow start` CLI
commands (ADR-020) without breaking backward compatibility.
